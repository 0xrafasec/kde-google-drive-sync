//! Google Drive Sync daemon — background service.

fn main() {
    // Bootstrap: config, DB, D-Bus, watcher, scheduler (Phase 2.4)
    eprintln!("gds-daemon: not yet implemented");
    std::process::exit(1);
}
